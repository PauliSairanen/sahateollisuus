class EventMetadata {
  constructor (id, name, eventImage, visibility) {
    this.id = id
    this.name = name
    this.eventImage = eventImage
    this.visibility = visibility
  }
}

export default EventMetadata